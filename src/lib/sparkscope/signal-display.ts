/**
 * 오늘의 시그널 패널이 화면에 실제로 깔는 기사 수.
 *
 * 이 값이 두 곳에서 함께 쓰인다:
 *  · 화면(SignalBanner) — 히어로 1건 + 스트립 (DISPLAY_COUNT - 1)건
 *  · 이름 카드(precompute-digest) — 카드를 만들 때 쓰는 기사 범위
 *
 * 둘이 어긋나면 카드에는 떠 있는데 목록에서는 찾을 수 없는 기사가 생긴다.
 * 실제로 그랬다(2026-09-10): 수집은 12건인데 화면은 7건만 깔았고, 카드는 12건 전부로
 * 만들어져서 "TechCrunch · 자기 발전하는 인공지능 경고, Anthropic 연구원 퇴사"가
 * 카드에만 있었다. 그래서 값을 한 곳에 두고 양쪽이 같이 참조한다.
 *
 * 서버 의존이 없는 파일로 따로 뺀 이유: 화면은 클라이언트 컴포넌트이므로 prisma나
 * OpenAI SDK를 끌어오는 모듈에서 값을 가져오면 클라이언트 번들에 그것들이 딸려
 * 들어간다(CLAUDE.md에 기록된 사고). 이 파일은 숫자 하나만 있고 import가 없다.
 */
export const DISPLAY_COUNT = 7;
