# ⚠️ 협업 개발 단계 전용 설정 (사내 실제 발표 전까지)
#
# DEV_AUTH_BYPASS=true 로 두면 매직 링크 로그인 없이
# 누구나 URL 접속 시 대시보드가 바로 뜹니다.
# (미들웨어 + dashboard/layout 의 로그인 검사를 건너뜀)
#
# 🔒 사내 실제 발표 전에 반드시:
#   - 이 파일을 삭제하거나 아래 값을 false 로 바꾸고
#   - git commit + push (Vercel 자동 재배포) 하면 로그인 보호가 복구됩니다.
#
# 참고: 이 값이 true 이면 대시보드 하단에 "🚀 고도화 미리보기(목업)"도 함께 표시됩니다.
DEV_AUTH_BYPASS=true
