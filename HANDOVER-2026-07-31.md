# SparkScope 인수인계 문서 (2026-07-31)

## 📌 이 문서의 목적

오늘(7/31) `Soyun-Branch`에 커밋된 모든 작업을 정리한다. 오늘 하루 **두 개의 세션이 같은 로컬 저장소를 동시에 사용**하면서 브랜치 체크아웃이 서로 꼬이는 사고가 있었고, 그 과정에서 이 세션이 만든 파일 일부가 디스크에서 유실됐다가 복구된 일이 있었다 — 아래 "오늘 있었던 사고" 섹션에 남겨둔다.

- 브랜치: `Soyun-Branch` (최신 커밋 `af94e1b`, origin에 push 완료)
- main과의 관계: 오늘 낮 `0717b33`에서 `Soyun-Branch`가 이미 main에 병합됨. 이후 작업은 계속 `Soyun-Branch`에서 진행 중이며 **아직 main에 재병합 안 됨**.

---

## 🚨 오늘 있었던 사고 — 동시 세션 브랜치 충돌

이 세션이 인터 탭 UI(히트맵)를 만드는 동안, 다른 세션이 백필 스크립트 작업을 마치고 `git checkout main` 등을 실행해서 **이 세션의 작업 디렉토리도 같이 main으로 넘어가는 일**이 있었다. 그 시점에 이 세션이 만들었던 미추적(untracked) 신규 파일 2개(`inter-insight.ts`, `api/inter/sector-urgency/route.ts`)가 디스크에서 사라졌다 — git에 add된 적이 없어서 커밋되지 않은 순수 새 파일이었기 때문. 대화 로그에 내용이 남아있어 그대로 복구해서 다시 커밋함 (`af94e1b`). **데이터 유실은 없었지만, 같은 로컬 저장소를 여러 세션이 동시에 조작하면 이런 일이 재발할 수 있으니 주의.**

---

## 오늘 커밋 전체 목록 (시간순)

| 시각 | 커밋 | 요약 |
|---|---|---|
| 09:56 | `6cc6889` | 경쟁사 고정 12개 카드도 대시보드 사전계산 대상에 포함 |
| 10:03~10:15 | `6b0a1e6`~`2652b17` | 대시보드 로딩 5초 병목 조사용 임시 계측 코드 추가/원복, Prisma `connection_limit` 명시 |
| 10:18~10:25 | `c88cb15`, `d74b72e`, `98c4cd6` | 진짜 병목이 Vercel 함수 리전(버지니아)과 DB(서울) 간 왕복 지연이었음을 특정 → 리전을 서울(`icn1`)로 프로젝트 전체 고정해서 해결 |
| 10:37, 10:50 | `f438742`, `29d9418` | 일일 수집 크론을 GitHub Actions에서 직접 실행하도록 변경, Node 20→22 (undici 버그 회피) |
| 13:05, 13:31 | `3801610`, `f62f337` | `leeryeong` 브랜치 병합 (다른 담당자 작업) |
| 13:28 | `af77599` | `backfill://` 더미 링크 클릭 시 빈 화면 뜨는 문제 전체 화면에 적용 |
| 14:20 | `fdc30d3` | **Inter 탭 RSS 수집 + Gemini 필터링 파이프라인 최초 구축** |
| 14:52 | `5f7dbfd` | **Inter 탭 포트폴리오 매칭 + 대시보드 연동 완성** (샘플 데이터 → 실제 파이프라인 전환 시작) |
| 14:58 | `1e4e235` | 포트폴리오 섹터 태깅 실험, 도메인 필터 프로바이더 비교 |
| 15:35 | `df39e4d` | `/api/inter` 라우트 신설 — `InterPanel.tsx`가 실제 DB를 읽도록 전환 |
| 15:49 | `6089a5e` | 기사별 섹터 자동 분류, 원문 링크 노출, 한국어 제목 번역 |
| 16:12 | `9137a6d` | **RSS 피드 URL 24개 중 다수가 죽어있던 것 수정** (21건 → 67건으로 증가), 기간 필터를 인트라 탭과 동일한 프리셋 버튼으로 교체 |
| 16:25 | `9b9e99f` | 인터 탭 중복 DB 조회 제거 (성능) |
| 17:32 | `d87eb58` | 인터 탭 과거 기사 백필 스크립트 추가 (⚠️ 아래 데이터 측면 참고 — 현재 그대로는 실행 불가) |
| 17:38 | `af94e1b` | **섹터 히트맵 + 배지 사유 AI 한 줄 요약** (이 세션 작업) |

---

## 데이터 측면 — 어디까지 했고 어디부터 시작해야 하는가

### 완료된 것
- **수집**: `inter-collect.ts` — 24개 매체(AI 7 + 바이오 5 + 오피니언/학술 5 + 종합경제 7) 중 21개 RSS가 정상 수집 중. 나머지 3개(The Information·Reuters·Washington Post·Scientific American 중 실제로는 4개가 코드 주석 처리돼있음)는 대체 피드를 못 찾아 보류 상태 — 이유가 코드 주석에 남아있음.
- **필터링**: `inter-filter.ts` — Gemini로 관련도 판정(`relevant`) + `domain`(bio/ai) + `sector` + 한국어 제목 번역(`titleKo`)까지 한 번에 처리.
- **포트폴리오 매칭**: `inter-portfolio-match.ts` — GPT로 스파크랩 포트폴리오사와의 연관성 매칭. 오늘 `max_completion_tokens`를 1000→3000으로 올려서 응답 잘림으로 인한 JSON 파싱 실패 문제 해결.
- **DB 현황** (오늘 직접 조회, 2026-07-31 17:40 기준):
  - `InterNews` 총 185건, 그중 Gemini 필터링 완료(`InterNewsVerdict`) 132건, 관련 있음(`relevant=true`) 67건 — 전부 `domain` 채워짐.
  - `InterPortfolioMatch` 70건.
  - 가장 오래된 기사: 2017-08-15(CNN) — **백필 스크립트가 실제로 이미 한 번 돌아서 과거 기사가 들어와 있음.**
  - DB에 `The Information`(1건), `Washington Post`(1건) 기사가 남아있는데, 지금 `FEEDS`에서는 이 두 매체가 주석 처리돼 더 이상 수집 안 됨 — 예전에 살아있을 때 들어온 잔여 데이터.
- **크론**: 일일 수집이 GitHub Actions(`daily-collect.yml`)에서 직접 도는 구조로 오늘 변경됨. (CLAUDE.md의 "발송 크론은 수집 안 함" 원칙과 무관한 별도 변경.)

### ⚠️ 지금 안 되는 것 / 다음에 먼저 확인할 것
1. **`scripts/backfill-inter-historical.ts`가 커밋된 그대로는 실행 안 됨.** `inter-collect.ts`에서 `FEEDS`, `decodeEntities`, `parseFeedItems`, `saveInterNewsIfNew`를 import하는데:
   - `FEEDS`/`decodeEntities`/`parseFeedItems`는 `inter-collect.ts`에 `export` 없이 선언돼있어 import 자체가 실패함.
   - `saveInterNewsIfNew`라는 함수는 `inter-collect.ts`에 아예 존재하지 않음(`collectInterNews`만 export됨).
   - 2017년 기사가 이미 들어와 있는 걸 보면 실제로는 한 번 돌긴 한 것 같은데, 그때 실행한 버전과 지금 커밋된 버전이 다른 것으로 보임. **다음 세션에서 가장 먼저 할 일: `inter-collect.ts`에 필요한 4개를 export 추가(또는 백필 스크립트를 현재 export에 맞게 수정)하고 나서 재실행.**
2. **국가별 필터(미국/중국/일본/사우디/전체)는 UI만 있고 실제로는 아무 효과 없음.** `/api/inter/route.ts`는 `domain`, `period`만 읽고 `country` 파라미터 자체를 받지 않음. 데이터 쪽에 국가 정보(어느 나라 매체/기사인지)가 아직 스키마에 없어서, 필터를 살리려면 먼저 기사별 국가 태깅부터 필요.
3. **`DOMAIN_SUMMARY`(바이오/AI 주요 요약 3줄)는 여전히 하드코딩된 샘플 문구.** 실제 기사 기반 AI 요약이 아님 — 대시보드의 `dashboard-insights.ts` 사전계산 패턴처럼 만들지, 이번에 추가한 `sector-urgency`처럼 요청 시점에 실시간으로 만들지 결정 필요.
4. **인터 탭은 아직 대시보드의 사전계산(`DashboardInsight`) 패턴을 안 씀.** 지금은 탭 열 때마다 `/api/inter`가 DB를 직접 조회(빠름, AI 호출 없음)하는 구조라 당장 문제는 없지만, `sector-urgency`(AI 호출 포함)는 매번 요청 시점에 gpt-4o-mini를 호출함 — 트래픽이 늘면 CLAUDE.md의 사전계산 패턴으로 옮겨야 함(코드 주석에 남겨둠).

---

## UI 측면 — 어디까지 했고 어디부터 시작해야 하는가

### 완료된 것
- **`InterPanel.tsx`가 샘플 데이터 → 실제 `/api/inter` 연동으로 전환 완료** (오늘 오후, `df39e4d`~`9b9e99f`). 바이오/AI 도메인 전환, 기간 프리셋(7일/1개월/3개월/1년/3년), 아코디언(기사·논문·오피니언 탭), 포트폴리오 매치, 원문 링크, 한국어 번역 제목까지 실제 DB 데이터로 렌더링됨.
- **오늘 이 세션에서 추가한 것 (`af94e1b`)**:
  - **분야별 이슈 강도 히트맵** — 섹터별로 가로 바 차트(바 길이=기사량, 색=배지 레벨: 긴급/모니터링/기회)를 아코디언 위에 추가해서 한눈에 어디가 뜨거운지 보이게 함.
  - **포트폴리오 매치 미리보기 칩** — 아코디언을 열지 않아도 히트맵 행에서 바로 매칭된 회사명이 보임 (예: "📎 지놈앤컴퍼니 외 1").
  - **히트맵 클릭 → 해당 섹터 아코디언 자동 오픈 + 스크롤 이동.**
  - **배지("긴급"/"모니터링"/"기회") 사유 한 줄 AI 요약** — 새 API(`/api/inter/sector-urgency`, `inter-insight.ts`)에서 gpt-4o-mini로 왜 그 배지인지 근거를 생성. 성공 시 "🤖 AI 요약 · ~", 실패/로딩 시 "⚙️ 기본 요약" 또는 "🤖 사유 분석 중…"으로 출처를 항상 구분해서 표시 (CLAUDE.md의 대시보드 요약 출처 구분 원칙을 인터 탭에도 그대로 적용).

### ⚠️ 다음에 먼저 확인할 것
1. **브라우저 실제 검증을 못 했다.** 다른 세션이 이미 3000번 포트에 dev 서버를 띄워둔 상태라 같은 저장소에 두 번째 서버를 겹쳐 띄우는 걸 피했음 — 타입체크(`tsc --noEmit`)는 통과했지만, 화면에서 히트맵/AI 요약이 실제로 잘 뜨는지는 **다음 세션에서 반드시 눈으로 확인 필요**. (그 전 라운드에서 샘플 데이터 버전으로는 브라우저 검증 완료했었음 — 실 데이터 버전에서는 아직임.)
2. **국가 필터 버튼을 눌러도 아무 반응 없음** (데이터 측면 2번과 동일 이슈 — UI는 있는데 API가 파라미터를 안 받음). 사용자가 헷갈릴 수 있어 우선순위 있으면 다음에 처리.
3. **히트맵의 "포트폴리오 매치" 칩은 첫 번째 회사만 보여주고 나머지는 "외 N"으로 뭉뚱그림** — 필요하면 호버 시 전체 리스트 툴팁 추가 고려.
4. 히트맵 바의 "기사량"은 현재 탭(전체) 아코디언의 `news+paper+opinion` 총합으로 계산 — 국가 필터가 살아나면 이 계산도 같이 필터링되게 손봐야 함.

---

## 다음 세션 시작 순서 (추천)

1. 데이터 측면 1번 — 백필 스크립트 export 버그부터 고친다 (안 그러면 과거 기사 추가 수집이 막혀있음).
2. UI 측면 1번 — 실 데이터 버전에서 히트맵 + AI 요약이 브라우저에서 정상 동작하는지 확인.
3. 국가 필터를 실제로 살릴지, 아니면 지금처럼 장식만 둘지 결정 (데이터 측면 2번, UI 측면 2번 — 같이 처리).
