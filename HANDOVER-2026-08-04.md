# SparkScope 인수인계 문서 (2026-08-04)

## 📌 이 문서의 목적

오늘 `Soyun-Branch`에 커밋된 작업(Inter 탭 UI 개편)을 정리한다. 오늘 안에 계정을 옮길 예정이라 다음 세션이 이 문서만 보고 이어갈 수 있게 남긴다.

- 브랜치: `Soyun-Branch`
- main과의 관계: **아직 main에 병합 안 됨.** 사용자가 직접 확인 후 병합할 예정.

---

## 🚨 가장 먼저 볼 것 — 프로덕션 DB에 다른 세션 작업이 이미 반영돼 있음

오늘 작업을 시작하기 전 `prisma migrate diff`로 확인한 결과, **`schema.prisma`에 없는 것들이 프로덕션 DB에 이미 존재했다**:

| 프로덕션에만 있던 것 | 상태 |
|---|---|
| `InterNewsVerdict.country` | 값 450건 채워져 있음 (us 330 / other 97 / cn 21 / jp 2), 기록 시각 2026-07-31~08-03 |
| `Bookmark` 테이블 | 행 0건, 참조 코드 없음 |

**이건 사용자가 다른 채팅 세션에서 진행 중이던 국가 분류·북마크 기능의 결과물**이다(같은 프로덕션 DB를 공유하는 별도 세션 작업, 다른 인턴 작업 아님). 그 세션의 코드는 아직 이 브랜치엔 없다. 이 상태에서 아무 생각 없이 `prisma db push`를 돌리면 두 개 다 **DROP**된다.

**오늘 한 조치:**
1. `schema.prisma`에 두 정의를 **선언만** 추가(주석에 출처·이유 명시) → 앞으로 `db push`를 돌려도 안 지워짐.
2. diff가 순수 추가(DROP 0건)로 좁혀진 걸 확인한 뒤, raw SQL(`ADD COLUMN IF NOT EXISTS`)로 `InterNewsVerdict`에 `isScrapped / scrappedAt / scrappedBy` 컬럼 3개 + 인덱스 1개 추가(아래 Inter 스크랩 기능용). 적용 전후 행 수(987건) 변화 없음 확인, 최종 diff 빈 것 확인.

**다음에 할 일:** 그 다른 세션의 국가 분류·북마크 작업이 이 브랜치와 합쳐질 때, `schema.prisma`에서 `country`/`Bookmark` 부분이 충돌 날 가능성이 높다 — 그때는 **두 쪽 필드의 합집합**으로 정리하면 된다(DB엔 이미 실제 컬럼이 존재하므로 스키마 파일만 맞추면 됨, 데이터 유실 위험 없음). Inter 탭 스크랩 기능은 `Bookmark`가 아니라 `InterNewsVerdict.isScrapped`를 쓰므로 그 둘은 서로 안 겹친다.

관련 메모리: `project_db_drift_country_bookmark.md`

---

## 오늘 커밋 내용 요약

Inter(해외 트렌드) 탭을 사용자 요청 6가지에 맞춰 개편:

1. **헤더 통일** — Intra와 동일하게 오늘 날짜·제목을 Inter 화면 상단에도 표시 (`GLOBAL TREND INTELLIGENCE`).
2. **스크랩 기능 추가** — `InterNewsVerdict.isScrapped`로 Inter 기사도 스크랩 가능(`/api/inter/scrap`, `InterScrapStar.tsx`). 스크랩함(`/dashboard/scraps`) 화면을 좌 Intra / 우 Inter 반반 분할로 재구성.
3. **기간 선택 UI 통일** — 별도 구현 대신 Intra의 `DateRangePicker`를 그대로 재사용(domain/country/scope는 URL 파라미터로 유지). 기존에 "UI만 있고 조회에 미반영"이었던 국가 필터를, 프로덕션에 이미 있던 `country` 컬럼을 활용해 실제로 연결했다.
4. **분류 기준 배지 버그 수정** — 섹터 배지("긴급"/"모니터링")가 실제로는 `idx % 2`, `idx % 3` 같은 **배열 인덱스로 정해지고 있었다**(항암은 항상 긴급, 신약발굴은 항상 모니터링). 그 옆 "AI 요약"은 이 가짜 배지를 정당화하는 문장을 지어내고 있었음. → 직전 동일 기간 대비 증감률·포트폴리오 매치 수·도메인 내 비중으로 실제 계산(`inter-sample-data.ts`의 `computeBadge()`), AI 프롬프트도 실제 집계 숫자를 받아 그 범위 안에서만 서술하도록 수정.
5. **AI 요약 가독성 개선 + 시각화 후보 3안 제시** — 하드코딩 3줄 요약(`DOMAIN_SUMMARY`, 데이터가 바뀌어도 문구가 고정)을 걷어내고 실제 집계값 기반 개요 블록(`OverviewSummary`)으로 교체. 별도로 트렌드 매트릭스(주제×사건유형) / 모멘텀 랭킹 / 포지셔닝 맵 3가지 후보를 더미 데이터로 만들어 아티팩트로 제시함(사용자가 아직 선택 안 함).
6. DB 작업은 위 "가장 먼저 볼 것" 참고.

### 변경된 파일
- `prisma/schema.prisma` — country/Bookmark 선언 + isScrapped 등 3컬럼
- `src/lib/inter-sample-data.ts` — 배지 계산 로직 전면 재작성(`computeBadge`, `buildOverview`), 국가 파라미터 반영
- `src/app/api/inter/route.ts` — from/to 기반 조회로 전환, country 파라미터 반영, overview 응답 추가
- `src/app/api/inter/sector-urgency/route.ts`, `src/lib/sparkscope/inter-insight.ts` — AI 요약 프롬프트에 실제 지표(`metricsLine`) 주입
- `src/app/api/inter/scrap/route.ts` (신규), `src/components/InterScrapStar.tsx` (신규) — Inter 스크랩 기능
- `src/components/InterPanel.tsx` — 국가 필터 실연결, URL 기반 상태 관리, 개요 블록·배지 근거 노출 추가
- `src/components/DateRangePicker.tsx` — `scope`/`extraParams` 지원 (Inter에서 재사용하기 위함)
- `src/app/dashboard/page.tsx` — 헤더를 Intra/Inter 공통으로 이동, Inter에도 `DateRangePicker` 적용
- `src/app/dashboard/scraps/page.tsx` — Intra/Inter 반반 분할 레이아웃

### 검증
- `tsc --noEmit`으로 타입체크 통과 (기존 scripts/ 폴더의 무관한 에러 제외).
- 로컬 dev 서버(`http://localhost:50284`, 포트는 3000이 사용 중이라 자동 할당됨)에서 브라우저로 직접 확인: Inter 탭 진입, 도메인/기간 전환, 섹터 아코디언 오픈, 스크랩 별표 클릭 → DB 기록 확인 → 스크랩함 화면에서 Inter 항목 표시까지 정상 동작.
- 테스트로 스크랩했던 기사 1건은 확인 후 원상복구(해제)함.

---

## 🔄 DB 병합 절차 (다음 세션: 계정 전환 후 1번부터 시작)

**배경:** 이 브랜치(`Soyun-Branch`)엔 스키마만 선언되어 있고, 다른 세션 브랜치엔 국가 분류·북마크 로직까지 전부 이미 붙어있을 것이다. 둘을 합칠 때 스키마 파일에서 충돌이 생길 가능성이 높다.

### 병합 순서 (1번부터 순서대로)

**1. 다른 세션 브랜치를 먼저 합치기**

```bash
# 예: 다른 세션이 eunbit-work 브랜치에서 작업했다면
git fetch origin
git merge origin/eunbit-work
# 또는 이 브랜치 위에 main을 먼저 올리고 싶으면
git merge origin/main
```

**2. schema.prisma 충돌 해결**

병합 후 `git status`를 보면 `schema.prisma`가 "both modified"로 뜬다. 이 파일을 열어서:

- **그쪽 세션 버전 유지:** `InterNewsVerdict.country`, `model Bookmark` 부분은 그쪽 코드 그대로 사용
- **이 쪽 추가분 놓치지 않기:** 이 브랜치에서 추가한 필드 3개를 같이 옮기기
  ```prisma
  // InterNewsVerdict 모델 안에 이 3줄이 **반드시 포함**되어야 함
  isScrapped Boolean  @default(false)
  scrappedAt DateTime?
  scrappedBy String?
  @@index([isScrapped])  // 이 인덱스도 함께
  ```
- 충돌 마크(`<<<<<<<`, `=======`, `>>>>>>>`) 모두 제거하고 합집합 상태로 정리

예시 (정리 후 최종 상태):
```prisma
model InterNewsVerdict {
  id        String   @id @default(cuid())
  newsId    String
  news      InterNews @relation(fields: [newsId], references: [id], onDelete: Cascade)
  relevant  Boolean
  reason    String
  domain    String?
  sector    String?
  titleKo   String?
  country   String?              // ← 그쪽 세션의 국가 분류
  model     String   @default("gemini-3.1-flash-lite")
  filteredAt DateTime @default(now())
  isScrapped Boolean  @default(false)    // ← 이 브랜치의 스크랩
  scrappedAt DateTime?                   // ← 이 브랜치의 스크랩
  scrappedBy String?                     // ← 이 브랜치의 스크랩
  matches   InterPortfolioMatch[]
  @@unique([newsId, model])
  @@index([filteredAt])
  @@index([relevant])
  @@index([domain])
  @@index([sector])
  @@index([isScrapped])  // ← 이 브랜치의 인덱스
}

model Bookmark {  // ← 그쪽 세션의 북마크 테이블 (이미 프로덕션에 있음)
  id        String   @id
  userId    String
  articleId String
  createdAt DateTime @default(now())
  @@unique([userId, articleId])
  @@index([userId])
}
```

**3. 스키마 재생성 및 타입 확인**

```bash
npx prisma generate
tsc --noEmit
```

**4. DB 푸시 (한 번만)**

```bash
# 충돌 해제 후 한 번만 돌리면 됨 (이미 프로덕션에 컬럼이 있으므로 no-op)
npx prisma db push
```

**5. 커밋**

```bash
git add schema.prisma
git commit -m "merge: 다른 세션 국가분류·북마크 브랜치 병합 + Inter 스크랩 필드 유지"
```

### 주의사항

- `db push`는 **이 병합이 완전히 끝난 뒤에만** 한 번만 돌린다(지금은 diff가 빈 상태라 급히 돌릴 필요 없음).
- 이 브랜치의 `isScrapped` 필드는 Inter 탭에서 스크랩 기능을 위해 반드시 남아야 함.
- 그쪽 `country` / `Bookmark`와 이 쪽 `isScrapped`는 서로 다른 도메인이므로 겹치거나 충돌하지 않음.

---

## ✅ 추가 완료 (2026-08-04 오후) — 축 분리 + 매트릭스 UI

위 "다음에 확인할 것"의 2·4번을 그대로 처리했다.

### 무엇을 했나

`sector` 컬럼 하나에 **주제**(항암·신약발굴 …)와 **사건유형**(규제·거버넌스, 투자·산업동향)이 섞여 있어서
"머크가 항암 신약사를 32조에 인수" 같은 기사가 항암이 아니라 투자로 분류됐다. 그 결과
투자·산업동향 혼자 relevant 450건 중 **151건(34%)**, 규제·거버넌스 46건까지 합쳐 **197건(44%)**을
차지하고 항암은 실제보다 작아 보였다. 축을 둘로 분리했다.

- **새 컬럼 2개** — `InterNewsVerdict.topicSector`(주제) / `eventType`(사건유형).
  기존 `sector`는 **건드리지 않고 그대로 보존**했다(되돌리기용). DROP 위험을 피하려고
  `prisma db push` 대신 raw SQL(`ADD COLUMN IF NOT EXISTS`)로 적용 — 적용 전후 행 수 987건 동일 확인,
  최종 `migrate diff` 빈 것 확인.
- **택소노미** — `INTER_EVENT_TYPES`(투자·딜 / 규제·승인 / 연구성과 / 제품·상용화 / 시장·인물),
  `BIO_TOPIC_SECTORS`·`AI_TOPIC_SECTORS`(cross-cutting 제외한 순수 주제 5개씩) 추가.
- **판정 프롬프트**(`inter-filter.ts`) — 신규 수집 기사부터 topicSector·eventType을 각각 채운다.
  "투자 기사·규제 기사도 그래서 어느 분야 이야기인가로 주제를 정한다"를 명시.
- **백필 완료** — `scripts/backfill-inter-event-type.ts`로 relevant 450건 전부 재분류(**450건 반영, 0건 실패**).
  재실행 안전(미분류 건만 처리), `--all`/`--limit N`/`--dry-run` 지원.

  | | 백필 전 (sector) | 백필 후 |
  |---|---|---|
  | 투자·산업동향 | 151건 (34%) | → 사건유형 `투자·딜` 63건으로 이동 |
  | 규제·거버넌스 | 46건 | → 사건유형 `규제·승인` 45건으로 이동 |
  | 신약발굴 | 70건 | **155건** (빨려나갔던 기사 회수) |
  | 항암 | 42건 | **49건** |

### UI (`InterPanel.tsx`)

- **헤드라인 4지표**(`HeadlineStats`) — 선별 기사+증감 / 가장 뜨거운 칸 / 포트폴리오 연결 / 우리와 겹치는 칸.
  "가장 뜨거운 칸"은 증감률로 고르되 **최소 3건** 문턱을 뒀다(0→1건이 무한대 증감률로 1위를 차지하는 것 방지).
- **주제 × 사건유형 매트릭스**(`SectorMatrix`) — 행=주제, 열=사건유형. **칸 하나하나가 버튼**이고
  누르면 그 조합만의 판정 근거·포트폴리오 매치·대표 기사 3건이 아래에 열린다(기사 없는 칸은 disabled).
  급증 칸은 빨간 링. 격자에 못 들어간 미분류 건수는 하단에 그대로 노출.
- **인사이트 패널**(`InsightPanel`) — 산점도(버블 그래프)는 요청대로 제거. 한 줄 / 우리 위치 /
  놓치기 쉬운 곳 / 액션 4줄 + 포트폴리오 매치 바 리스트(보라색). 전부 집계값이라 AI 호출 없음.
- **아코디언**도 주제 축(`topicSector`)으로 통일 — 매트릭스와 같은 축이라야 화면이 일관된다.

### 검증
- `tsc --noEmit` 통과(기존 `scripts/send-staff-digest.ts` 에러 1건은 이 변경 전에도 있던 것).
- 바이오·AI 두 도메인 모두 브라우저 확인: 매트릭스 렌더, 칸 클릭 상세(신약발굴×투자·딜 → J&J의 Sail 인수 등
  실제 투자 기사, 항암×규제·승인 → FDA 자문위 기사 3건), 도메인 전환, 인사이트 패널 정상.
- 콘솔 에러는 기존 `ScrollTopButton` SVG 속성 경고뿐(무관).

---

## ✅ 추가 완료 (2026-08-04 저녁) — 수집 공백 해소 + 조회 UX + 색/말투/폰트

### "발행일 깨진 454건"은 오진이었다 — 진짜 원인은 2개월 수집 공백

이전 기록에 "발행일이 깨진 454건"이라고 적었는데 **틀렸다.** 실제로 확인해 보니 그 454건 중
423건은 2025년 기사로 **날짜가 정상**이다(예: 2025-01-27 WSJ의 DeepSeek 기사, 2025-11-05
TechCrunch의 Apple·Google Siri 기사 — 다 실제 그 날짜 기사). 진짜로 값이 이상한 건 2014~2024년
30여 건뿐이고, 그중엔 채널 제목이 기사로 저장된 것도 있다(CNN "CNN.com - RSS Channel").

진짜 문제는 따로 있었다. **Inter 수집은 2026-07-31에 처음 돌았다**(전체 1046건이 7/31~8/3 사이
`collectedAt`). 그리고 `backfill-inter-historical.ts`는 "최근 90일은 정규 RSS가 커버한다"고 보고
그 구간을 **의도적으로 건너뛴다**. 그런데 RSS는 매체당 최신 20~50개(≈2~3주치)만 준다.
→ 백필이 덮은 구간(~2026-05-05)과 RSS가 덮은 구간(≈2026-07-15~) 사이 **2개월이 통째로 비어 있었다.**

| relevant 월별 | 조치 전 | 조치 후 |
|---|---|---|
| 2026-04 | 41건 | 41건 |
| 2026-05 | **6건** | **253건** |
| 2026-06 | **0건** | **168건** |
| 2026-07 | 63건 | **202건** |
| 최근 90일 합계 | **63건** | **515건** |
| 최근 1년 합계 | 426건 | **984건** |

- `backfill-inter-historical.ts`에 `--after/--before/--chunk-days/--items` 옵션을 추가해 임의 구간을
  직접 메울 수 있게 했다(URL 중복은 `saveInterNewsIfNew`가 막으니 겹쳐 돌려도 안전).
  실행: `npx tsx scripts/backfill-inter-historical.ts --after=2026-05-01 --before=2026-07-25 --chunk-days=20 --items=14`
- 새로 판정된 기사는 **주제·사건유형이 자동으로 채워졌다**(신규 relevant 356건 중 356건 eventType,
  348건 topicSector) — 프롬프트 수정이 실제로 동작함을 확인. 별도 eventType 백필이 필요 없다.

### 조회 조건 UX
- 조회기간을 헤더 아래에서 **국가 필터와 같은 흰 카드 안**(국가별 트렌드 바로 위)으로 옮겼다.
- 기간·국가는 클릭 즉시 조회되지 않는다 — **선택은 하이라이트만 즉시 반영**되고, 카드 오른쪽 아래
  **'확인'**을 눌러야 URL이 바뀌며 조회된다(변경 없으면 확인 비활성). 안내 문구도 카드 안에 넣었다.
- `DateRangePicker`는 Intra와 계속 공유한다(CLAUDE.md "별도 구현 금지"). `onStage` 콜백이 있으면
  URL을 안 바꾸고 선택만 올려보내는 모드로 동작하도록 확장했고, `accent` prop으로 색만 갈아끼운다.

### 색·말투·폰트
- **Inter 탭은 초록 계열로 통일**(앞으로 수정할 때도 보라 대신 초록) — 기간 프리셋 하이라이트,
  포트폴리오사 막대, AI 도메인 탭. Intra는 보라 유지.
- 3줄 요약·배지 사유 프롬프트에 **"~습니다/~합니다" 종결형** 규칙 명시. 이미 저장돼 있던 문구는
  `scripts/refresh-inter-summary.ts`로 즉시 재계산(다음 배치까지 안 기다리게).
- Inter 탭 폰트 한 단계씩 확대(10→11, 11→12, xs→13, sm→15 …).

### '급증' 배지 기준 강화 (중요)
공백을 메우고 나니 **25칸 중 20칸에 빨간 '급증' 링**이 붙었다. 이번 기간만 촘촘하게 백필돼
직전 기간이 대부분 0건이었고, `prevCount === 0`을 "폭증"으로 처리했기 때문이다. 예전 가짜 배지와
같은 문제라 규칙을 고쳤다 — **'급증'은 `prevCount > 0`이고 증감 +50% 이상, 3건 이상일 때만** 붙는다.
"가장 뜨거운 칸"도 비교 가능한 칸에서만 고른다(없으면 건수 1위로 대체). 급증 칸 20개 → 11개.

---

## ⚠️ 다음에 먼저 확인할 것

1. **증감률은 아직 신뢰하지 말 것 (가장 중요)** — 이번 기간(2026-05~07)만 새로 백필돼서
   직전 기간(2026-02~05)보다 수집 밀도가 훨씬 높다. 그래서 화면의 `▲412%`, `▲525%` 같은 값은
   **실제 시장 변화가 아니라 수집 깊이의 차이**를 반영한다. 비교를 공정하게 만들려면 직전 구간도
   같은 깊이로 백필해야 한다:
   `npx tsx scripts/backfill-inter-historical.ts --after=2026-02-01 --before=2026-05-01 --chunk-days=20 --items=14`
2. **포트폴리오 매칭이 아직 도는 중** — 위 백필의 마지막 단계(GPT 매칭)가 세션 종료 시점에도
   진행 중이었다. `InterPortfolioMatch` 건수가 302에서 더 늘어나야 정상이다. 안 늘었으면
   `matchInterNewsWithPortfolio`를 relevant verdict에 대해 다시 돌린다.
3. **중국·일본·사우디가 거의 안 잡힌다** — 원인은 분류가 아니라 **소스 구성**이다. `FEEDS` 21개가
   전부 미국·영국 매체(TechCrunch·WSJ·FT·Nature·STAT …)여서, 그 나라 기사는 서구 매체가 그 나라를
   다룰 때만 잡힌다. 최근 1년 기준 us 679 / other 250 / **cn 49 / jp 5 / sa 1**.
   → 해결하려면 현지 매체를 `FEEDS`에 추가해야 한다(중국: SCMP·TechNode·Caixin Global,
   일본: Nikkei Asia, 중동: Arab News·Al Arabiya). 추가 안 할 거면 사우디 탭은 빼는 게 정직하다.
4. **위 DB drift 병합** — 다른 세션의 국가 분류·북마크 작업이 합류할 때 `schema.prisma` 충돌 예상. 합집합으로 정리.
5. **미분류 잔여** — 도메인 전반 기사라 주제를 특정할 수 없는 건은 `topicSector=null`로 남아 격자에서
   빠진다(3개월 조회 시 9건). 화면 하단에 건수로 그대로 노출 중이니 숨겨진 누락은 아니다.
6. `stats`(`getDomainStats`)는 헤드라인 4지표로 대체돼 화면에서 안 쓴다 — API 응답에만 남아 있음.
7. 값이 실제로 이상한 30여 건(2014~2024년, 채널 제목이 기사로 저장된 것 포함) 정리 — 우선순위 낮음.
8. 도메인 누수(바이오 매체 기사가 `domain=ai`로 분류된 사례 소수) — 우선순위 낮음.
