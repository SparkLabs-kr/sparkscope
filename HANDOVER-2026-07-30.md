# SparkScope 인수인계 문서 (2026-07-30)

## 📌 이 문서의 목적

계정을 옮겨서 작업을 이어가기 위한 문서입니다. **이번 세션에서는 코드를 실제로 수정한 것은 없고, 전부 진단·설계·논의만 진행했습니다.** 아래 두 가지 작업이 진행 중(미완료) 상태로 남아있습니다.

- 브랜치: `Soyun-Branch` (최신 커밋 `44c9d43`, origin/main과 동일 — 커밋 차이 없음)

---

## 🚨 가장 먼저 확인할 것 — 커밋 안 된 작업이 로컬에 있음

이번 세션 도중 발견한 것으로, **이전 대화(다른 세션)에서 이미 작업 중이던 "Inter(해외 트렌드) 탭" 코드가 커밋되지 않은 채 로컬에 남아있습니다.** git이 추적하지 않는 파일이라 지난번 코드 조사에서 놓쳤던 부분입니다. 계정을 옮기기 전에 **반드시 커밋하거나 최소한 다른 곳에 백업**해두는 걸 권장합니다 (untracked 파일은 실수로 `git clean` 등을 하면 영구 삭제됩니다).

### 미추적(untracked) 파일
- `src/components/InterPanel.tsx` (264줄) — Inter 탭 UI. 바이오/AI 도메인 전환, 국가별(미국·중국·일본·사우디·전체) 탭 구현됨. 주석: "아직 해외 소스 수집 파이프라인이 없어 전부 샘플 데이터로 UI만 구현한 상태"
- `src/lib/sparkscope/inter-sample-data.ts` (255줄) — 위 UI가 쓰는 더미 데이터. 주석: "바이오는 이령, AI는 소윤이 각자 소스 목록을 정해 붙일 예정 — CLAUDE.md 참고" (※ 현재 CLAUDE.md에는 이 계획이 아직 반영 안 되어 있음 — 확인 필요)

### 커밋 안 된 수정(modified) 파일
- `src/app/dashboard/page.tsx` — Intra(🏠)/Inter(🔭) 스코프 전환 UI가 이미 연결되어 있음. `?scope=intra|inter` URL 파라미터로 전환, `scope=inter`일 때 `<InterPanel />`을 렌더링. `git diff src/app/dashboard/page.tsx`로 전체 diff 확인 가능(47줄 추가/3줄 삭제).

### ⚠️ 알아둘 것
- `scope=inter`로 전환해도 **`loadDashboardData()`(인트라 탭 전체 데이터 계산)가 여전히 무조건 먼저 실행됩니다.** Inter 탭은 정작 이 데이터를 안 쓰는데도 비용을 그대로 지불하는 구조 — 아래 "대시보드 속도 개선" 작업과 반드시 같이 고려해야 함.
- 이전에 만들어 전달한 PDF 리포트("SparkScope_프로젝트_현황_리포트.pdf")의 8번 항목("intra/inter 탭 없음")은 **이 미추적 파일들을 놓쳐서 생긴 오류**입니다. 실제로는 이미 작업 중인 코드가 있습니다.

---

## 진행 중 작업 1 — 경쟁사 탭 "언론노출 상위" 개선 (4개 요청사항)

**상태: 진단만 완료, 구현 시작 안 함.**

| # | 요청 | 확인 결과 |
|---|---|---|
| 1 | 지정한 회사가 상위 노출되게 | [page.tsx:231](src/app/dashboard/page.tsx#L231) `sort((a,b)=>b.count-a.count).slice(0,10)` — 순수 기사 건수 TOP10, 우선 노출 로직 없음 |
| 2 | 카카오벤처스·IMM인베스트먼트 키워드 추가, 에이티넘 tier=1 채우기 | `data/master-keywords.json`에 카카오벤처스·IMM 자체가 없음(확인함). 에이티넘은 있으나 `tier: null`. **아직 미반영** |
| 3 | tier가 노출/정렬에 실제로 반영되게 | `CompetitorPanel.tsx`, `competitor-insights.ts`, `dashboard/page.tsx` 어디에도 tier 참조 없음(확인함) — tier는 `collector.ts`의 수집 단계에서만 쓰이고 노출 순위와는 무관. **아직 미반영** |
| 4 | tier 필터 켤 때 C tier(포트폴리오 복원 35개 포함)가 사라지지 않는지 확인 | tier 필터 자체가 대시보드에 없어 아직 검증 대상이 없음. **"포트폴리오 복원 35개"가 정확히 뭘 가리키는지 코드에서 특정 못 함 — 소윤님께 재확인 필요** |

**다음 세션에서 할 일**: "35개 복원"이 뭘 가리키는지 확인 → 2·3·4번 순서로 구현 여부 확정.

---

## 진행 중 작업 2 — 대시보드 속도 개선 (메인 작업, 이수님 답변 대기 중)

**상태: 근본 원인 진단 완료 → A안(사전계산 전환) 설계안 작성 → 3가지 결정사항 추천안까지 만들어서 이수님께 전달함. 답변 대기 중.**

### 느린 이유 (진단 완료)
- `dashboard/page.tsx`가 `export const dynamic = 'force-dynamic'` — 탭(`?tab=`)을 클릭할 때마다 **탭과 무관하게 전체 데이터·AI 요약을 처음부터 재계산**
- [analyzer.ts:275](src/lib/sparkscope/analyzer.ts#L275) `summarizeCrisisCause` — 위기 원인 AI 요약, **캐시 전혀 없음**, 헤더 영역(모든 탭 공통)에 있어서 매 클릭마다 재호출
- `competitor-insights.ts` — 경쟁사 트렌드 AI 요약, 하루 1회 메모리 캐시는 있지만 Vercel 서버리스 특성상 콜드스타트마다 비워짐, 경쟁사 탭이 아니어도 무조건 실행
- 위 "미추적 파일" 섹션 참고 — `scope=inter`에서도 이 무거운 계산이 그대로 실행됨

### 합의된 방향 (A안)
AI 요약을 페이지 렌더링 시점이 아니라 **크론에서 미리 계산해 DB에 저장, 페이지는 읽기만** 하는 구조로 전환. (다이제스트 발송이 이미 이 패턴 — `daily-collect.yml`이 미리 계산해두고 발송 크론은 읽기만 함)

### 이수님께 전달한 3가지 결정사항 + 추천안

1. **신규 키워드 추가 시 백필 시점** → 추천: **다음 정기 크론까지 대기** (휴리스틱 폴백 `crisisFallbackCause()` 재사용, 비용 0원)
2. **폴백(휴리스틱) 결과를 화면에 티 낼지** → 추천: **명시적으로 표시** ("원인 요약 · OO시 기준" 등, AI 분석인지 기본 문구인지 구분)
3. **크론이 통째로 실패했을 때** → 추천: **그날은 실시간 AI 호출로 자동 폴백** (화면은 평소와 동일하게 정상 작동, 장애는 RunLog에만 기록)

**크론 주기 원칙**(이수님 의견 반영): `daily-collect.yml` 끝에 요약 단계 추가, 기본 하루 1회. 처음부터 낮 시간대 추가 실행 넣지 않고, "요약이 오래됐다"는 실제 불만이 나오면 그때 늘림. → **CLAUDE.md 크론 섹션에 이 원칙을 문서화하는 것도 다음 할 일.**

### 신선도 설계 원칙
| 요소 | 출처 | 신선도 |
|---|---|---|
| 위기 급증 여부·건수·기사 목록 | DB 직접 조회 | 실시간 (그대로 유지) |
| 위기 원인 한 줄 / 경쟁사 트렌드 3줄 | 사전계산 테이블 | 크론 주기(최대 24시간) |

### 다음 세션에서 할 일
1. **이수님 답변이 왔는지부터 확인** (이 대화를 이어받은 사람이 제일 먼저 할 일)
2. 답변 받으면 3가지 결정 확정
3. 확정되면: `DashboardInsight` 류의 새 테이블 스키마 설계 → `daily-collect.yml`에 요약 계산 단계 추가 → `dashboard/page.tsx`를 "테이블 읽기"로 전환 → 이때 **Inter 스코프도 같은 원칙으로 데이터 로딩을 분리**(위 미추적 파일 이슈와 함께 처리)
4. CLAUDE.md 크론 섹션에 "요약 크론은 기본 1일 1회, 불만 생기면 그때 늘림" 원칙 추가

---

## ✅ 다음 세션 체크리스트

- [ ] `git status`로 미추적 파일(InterPanel.tsx 등) 그대로 있는지 확인, 백업/커밋 여부 결정
- [ ] 이수님이 3가지 결정사항에 답변했는지 확인
- [ ] "포트폴리오 복원 35개" 용어 확인
- [ ] 경쟁사 탭 개선 4개 항목 구현 여부 확정
- [ ] 대시보드 속도 개선(A안) 스키마·크론 설계 구체화

---

## 📚 참고

- 이전에 만든 종합 리포트: `SparkScope_프로젝트_현황_리포트.pdf` (프로젝트 전체 구조 + 세션 17건 이력, 8번 항목은 위 정정 참고)
- CLAUDE.md — 브랜치 정책·크론 원칙·키워드 마스터 시트 규칙

---

**작성 시각:** 2026-07-30 (세션 종료 시점)
**작성자:** Claude Code
